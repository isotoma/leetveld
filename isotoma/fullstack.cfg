[buildout]
directory = /var/local/sites/${names:primaryurl}
develop =
parts-directory = ${:directory}
installed = ${:directory}/installed.cfg

[environment]
extra-paths = ${project:projectpath}
sitename = ${names:primaryurl}
