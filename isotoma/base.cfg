[buildout]
develop =
    thirdparty/gae2django
    src
extensions = buildout.dumppickedversions
eggs =
    leetveld 
    django-gae2django
parts =
    bootstrap
    django
find-links = http://pypi.python.org/packages/source/D/Django/Django-1.2.3.tar.gz

[bootstrap]
recipe = collective.recipe.bootstrap

[django]
recipe = djangorecipe
version = 1.2.3
settings = settings
projectegg = codereview
eggs = ${buildout:eggs}
