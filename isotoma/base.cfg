[buildout]
extensions =
    isotoma.stack.django
develop =
    src
    thirdparty/django-gae2django
versions = versions

[isotoma.stack.django]
environment = dev
project = leetveld

[environment]
extra-paths = ${buildout:cwd}/src/leetveld

[project]
media-dir = static
admin-media = /media

[versions]
isotoma.stack.django = 0.0.8
Django = 1.2.5
